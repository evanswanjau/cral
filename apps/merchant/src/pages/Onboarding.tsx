/**
 * Landing point right after sign-up verifies, instead of dropping someone
 * straight into an empty dashboard or back at the sign-in form. Company
 * papers and vehicle intake are later-phase work (see CLAUDE.md's phase
 * sequencing) — this is a placeholder waypoint, not a built flow yet.
 */
export function Onboarding(): JSX.Element {
  return (
    <div className="flex min-h-screen items-center justify-center bg-white">
      <p className="text-lg font-medium text-slate-900">Onboarding page — coming soon.</p>
    </div>
  );
}
